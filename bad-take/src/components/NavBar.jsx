/*sticky nav bar at the top, has light or darkmode, has website name and logo, has navigation to home, has login and sign up bottom if not logged in and has a logout button if signed in*/
const NavBar = () =>{
    return(
        <div>

        </div>
    )
}

export default NavBar